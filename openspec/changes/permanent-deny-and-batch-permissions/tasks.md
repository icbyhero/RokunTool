# 任务清单: 永久拒绝和批量权限请求

## Phase 1: 永久拒绝功能 (P0 - 核心功能)

### 1.1 权限系统核心

- [ ] **任务 1.1.1**: 添加 `PERMANENTLY_DENIED` 状态
  - 文件: `src/main/permissions/permission-manager.ts`
  - 在 PermissionStatus 枚举中添加 `PERMANENTLY_DENIED = 'permanently_denied'`
  - 更新 TypeScript 类型定义

- [ ] **任务 1.1.2**: 更新 `PermissionStore` 数据结构
  - 文件: `src/main/permissions/permission-store.ts`
  - 将权限值从字符串改为对象,包含 status 和 permanent 标志
  - 添加从 v1 到 v2 的数据迁移逻辑
  - 测试现有数据的迁移

- [ ] **任务 1.1.3**: 为 `denyPermission()` 方法添加 `permanent` 参数
  - 文件: `src/main/permissions/permission-manager.ts`
  - 签名: `async denyPermission(pluginId, permission, source, permanent, context?)`
  - 如果 `permanent === true`,设置状态为 PERMANENTLY_DENIED 并持久化
  - 如果 `permanent === false`,保持当前行为(仅会话级拒绝)

- [ ] **任务 1.1.4**: 更新 `checkPermission()` 返回 PERMANENTLY_DENIED 状态
  - 文件: `src/main/permissions/permission-manager.ts`
  - 检查 `permissionStates` 中的 PERMANENTLY_DENIED 状态
  - 返回状态给调用者(而不仅仅是布尔值)

- [ ] **任务 1.1.5**: 更新 `requestPermission()` 处理永久拒绝的权限
  - 文件: `src/main/permissions/permission-manager.ts`
  - 显示对话框前检查权限是否为 PERMANENTLY_DENIED
  - 如果永久拒绝:
    - 不显示对话框
    - 返回 `{ granted: false, deniedPermanently: true }`
    - 发出 `permission:permanently-denied` 事件

- [ ] **任务 1.1.6**: 添加批量权限检查的 IPC 处理器
  - 文件: `src/main/ipc/handlers.ts`
  - 添加 `permission:checkPermissions` 处理器用于批量检查
  - 返回权限状态数组

### 1.2 UI 组件

- [ ] **任务 1.2.1**: 更新 `PermissionRequestDialog` 为四按钮布局
  - 文件: `src/renderer/src/components/permissions/PermissionRequestDialog.tsx`
  - 从 3 个按钮改为 4 个按钮:
    - 拒绝 (会话级拒绝)
    - 永久拒绝 (永久拒绝)
    - 本次授权 (会话级授权)
    - 永久授权 (永久授权)
  - 更新 `onResponse` 回调包含 `permanent` 标志
  - 更新每个选项的详细说明

- [ ] **任务 1.2.2**: 更新 IPC 桥接支持 `permanent` 标志
  - 文件: `src/preload/ipc.ts`
  - 更新 `PermissionApi.sendResponse()` 接口包含 `permanent?: boolean`
  - 更新类型定义

- [ ] **任务 1.2.3**: 更新 `App.tsx` 传递 `permanent` 标志
  - 文件: `src/renderer/src/App.tsx`
  - 更新 `handlePermissionResponse()` 接受并传递 `permanent` 参数

- [ ] **任务 1.2.4**: 创建 `PermissionDeniedToast` 组件
  - 文件: `src/renderer/src/components/permissions/PermissionDeniedToast.tsx` (新建)
  - 当权限被永久拒绝时显示 Toast 通知
  - 显示插件名称、权限名称和操作上下文
  - 包含"前往设置"按钮打开设置页面

- [ ] **任务 1.2.5**: 添加永久拒绝通知的 IPC 事件监听器
  - 文件: `src/renderer/src/App.tsx`
  - 监听 `permission:permanently-denied` 事件
  - 收到事件时显示 `PermissionDeniedToast`
  - 节流通知: 同一个(插件, 权限)组合每个会话只通知一次

### 1.3 设置页面

- [ ] **任务 1.3.1**: 创建 `PermissionSettings` 组件
  - 文件: `src/renderer/src/components/pages/PermissionSettings.tsx` (新建)
  - 列出所有插件及其权限
  - 显示每个权限的状态 (GRANTED, DENIED, PERMANENTLY_DENIED)
  - 为已授予权限添加"撤销"按钮
  - 为永久拒绝权限添加"重新询问"按钮

- [ ] **任务 1.3.2**: 添加清除永久拒绝的方法
  - 文件: `src/main/permissions/permission-manager.ts`
  - 添加 `clearPermanentDeny(pluginId, permission)` 方法
  - 从 permissionStates 中移除 PERMANENTLY_DENIED 状态
  - 重置为 PENDING,下次请求时会显示对话框

- [ ] **任务 1.3.3**: 添加 `permission:clearPermanentDeny` IPC 处理器
  - 文件: `src/main/ipc/handlers.ts`
  - 处理 `permission:clearPermanentDeny` 请求
  - 调用 `permissionManager.clearPermanentDeny()`

- [ ] **任务 1.3.4**: 添加设置菜单项
  - 文件: `src/renderer/src/components/layout/Sidebar.tsx` (或类似文件)
  - 添加"权限管理"菜单项
  - 导航到 `PermissionSettings` 页面

- [ ] **任务 1.3.5**: 从 Toast 添加导航到设置
  - 文件: `src/renderer/src/components/permissions/PermissionDeniedToast.tsx`
  - 实现 `onOpenSettings` 处理器
  - 导航到权限设置页面
  - 高亮相关的插件/权限

### 1.4 测试

- [ ] **任务 1.4.1**: PermissionManager 单元测试
  - 测试 PERMANENTLY_DENIED 状态
  - 测试永久拒绝 vs 临时拒绝
  - 测试清除永久拒绝
  - 测试权限检查返回正确状态

- [ ] **任务 1.4.2**: PermissionStore 单元测试
  - 测试 v1 到 v2 数据迁移
  - 测试读写永久拒绝状态
  - 测试跨应用重启的持久化

- [ ] **任务 1.4.3**: 永久拒绝流程集成测试
  - 用户永久拒绝权限
  - 插件尝试使用权限
  - 验证显示 Toast 通知
  - 用户在设置中清除永久拒绝
  - 验证再次显示权限请求对话框

## Phase 2: 批量权限请求 (P1 - 完整体验)

### 2.1 权限系统 API

- [ ] **任务 2.1.1**: 添加 `checkPermissions()` 方法用于批量检查
  - 文件: `src/main/permissions/permission-manager.ts`
  - 方法: `async checkPermissions(permissions: Permission[]): Promise<PermissionCheckResult>`
  - 返回:
    - `hasPermanentDeny: boolean`
    - `permanentlyDenied: Permission[]`
    - `pending: Permission[]`
    - `granted: Permission[]`
  - 不显示对话框,仅检查状态

- [ ] **任务 2.1.2**: 添加 `requestPermissions()` 方法用于批量请求
  - 文件: `src/main/permissions/permission-manager.ts`
  - 方法: `async requestPermissions(permissions: Permission[], reason?, context?): Promise<BatchPermissionResult>`
  - 显示单个对话框包含所有权限
  - 返回每个权限的结果
  - 支持部分授权(用户选择部分但不全部)

- [ ] **任务 2.1.3**: 更新权限请求对话框支持批量
  - 文件: `src/main/permissions/permission-manager.ts`
  - 传递权限数组到渲染进程
  - 处理数组响应,每个权限单独的授权/拒绝

- [ ] **任务 2.1.4**: 添加批量操作的 IPC 处理器
  - 文件: `src/main/ipc/handlers.ts`
  - 添加 `permission:checkPermissions` 处理器
  - 添加 `permission:requestPermissions` 处理器
  - 更新 preload 中的 `PermissionApi` 接口

### 2.2 UI 组件

- [ ] **任务 2.2.1**: 更新 `PermissionRequestDialog` 显示多个权限
  - 文件: `src/renderer/src/components/permissions/PermissionRequestDialog.tsx`
  - 显示权限列表而不是单个权限
  - 每个权限显示:
    - 名称和描述
    - 风险等级图标
    - 授权/拒绝复选框
  - 添加"全选" / "全不选" / "取消选中"按钮
  - 更新为处理 `PermissionRequest[]` 而不是单个请求

- [ ] **任务 2.2.2**: 添加权限选择状态管理
  - 文件: `src/renderer/src/components/permissions/PermissionRequestDialog.tsx`
  - 跟踪哪些权限被选中
  - 允许用户切换单个权限
  - 显示已选权限的数量

- [ ] **任务 2.2.3**: 更新批量权限的响应处理
  - 文件: `src/renderer/src/components/permissions/PermissionRequestDialog.tsx`
  - 发送响应数组回主进程
  - 包含每个权限的 `granted` 和 `permanent` 标志

- [ ] **任务 2.2.4**: 更新 `App.tsx` 处理批量权限请求
  - 文件: `src/renderer/src/App.tsx`
  - 更新 `PermissionRequest` 类型支持权限数组
  - 传递数组到 `PermissionRequestDialog`

### 2.3 插件集成

- [ ] **任务 2.3.1**: 更新插件基类添加批量权限辅助方法
  - 文件: `src/main/plugins/base-plugin.ts` (或类似文件)
  - 添加 `checkPermissions()` 包装方法
  - 添加 `requestPermissions()` 包装方法
  - 添加 TypeScript 文档和示例

- [ ] **任务 2.3.2**: 创建使用批量权限的示例插件
  - 文件: `plugins/example-batch-permissions/index.js` (新建)
  - 演示预检查模式
  - 演示批量请求模式
  - 演示错误处理

### 2.4 测试

- [ ] **任务 2.4.1**: 批量权限检查单元测试
  - 测试检查多个权限
  - 测试正确分类(永久拒绝 / 待确认 / 已授予)
  - 测试空权限数组

- [ ] **任务 2.4.2**: 批量权限请求单元测试
  - 测试请求多个权限
  - 测试部分授权(部分授予,部分拒绝)
  - 测试全部授予 / 全部拒绝场景

- [ ] **任务 2.4.3**: 批量权限流程集成测试
  - 插件预检查权限,发现全部待确认
  - 系统显示批量对话框
  - 用户授予部分,拒绝其他
  - 验证返回正确结果
  - 插件仅基于已授予权限执行

## Phase 3: 优化和完善 (P2 - 优化)

### 3.1 用户体验

- [ ] **任务 3.1.1**: 添加权限风险指示器
  - 文件: `src/renderer/src/components/permissions/PermissionRequestDialog.tsx`
  - 为每个权限显示风险等级图标 (🟢 低 / 🟡 中 / 🔴 高)
  - 为权限项进行颜色编码

- [ ] **任务 3.1.2**: 在批量对话框中添加权限描述
  - 文件: `src/renderer/src/components/permissions/PermissionRequestDialog.tsx`
  - 显示每个权限允许做什么
  - 显示插件为什么需要它(来自 `reason` 参数)
  - 显示潜在风险

- [ ] **任务 3.1.3**: 改进权限被拒 Toast 消息
  - 文件: `src/renderer/src/components/permissions/PermissionDeniedToast.tsx`
  - 显示更可操作的建议
  - 链接到权限系统的文档
  - 显示历史(何时被拒绝)

### 3.2 性能

- [ ] **任务 3.2.1**: 优化权限检查缓存
  - 文件: `src/main/permissions/permission-manager.ts`
  - 缓存批量检查结果
  - 权限变更时使缓存失效

- [ ] **任务 3.2.2**: 防抖权限被拒通知
  - 文件: `src/renderer/src/App.tsx`
  - 每个会话每个(插件, 权限)只显示一个通知
  - 在设置中添加"显示所有被拒权限"链接

### 3.3 文档

- [ ] **任务 3.3.1**: 编写插件开发者指南
  - 文件: `docs/plugin-development.md` (新建)
  - 解释权限系统
  - 展示推荐模式
  - 提供代码示例

- [ ] **任务 3.3.2**: 编写权限管理用户指南
  - 文件: `docs/user-guide/permissions.md` (新建)
  - 解释权限类型
  - 解释如何管理权限
  - 常见问题故障排除

### 3.4 测试

- [ ] **任务 3.4.1**: 完整工作流程的端到端测试
  - 用户安装插件
  - 用户使用插件功能
  - 授予权限
  - 之后撤销权限
  - 验证插件处理撤销

- [ ] **任务 3.4.2**: 批量权限性能测试
  - 测试 10+ 个权限
  - 测量对话框渲染时间
  - 测量权限检查时间
  - 必要时优化

## 验证清单

对于每个任务,验证:

- [ ] 代码遵循项目风格指南
- [ ] TypeScript 类型正确
- [ ] 实现了错误处理
- [ ] 用户可见的字符串为中文
- [ ] 单元测试通过
- [ ] 集成测试通过
- [ ] 完成手动测试
- [ ] 更新了文档

## 依赖关系

- 任务 1.x (永久拒绝) 必须在 任务 2.x (批量权限) 之前完成
- 任务 2.x (批量权限) 必须在 任务 3.x (优化) 之前完成
- 任务 1.3.1 (PermissionSettings) 依赖于 任务 1.2.4 (PermissionDeniedToast) 用于导航

## 预估工作量

- Phase 1: 约 3-4 天
- Phase 2: 约 4-5 天
- Phase 3: 约 2-3 天

**总计**: 约 9-12 天

## 发布计划

1. **Alpha**: Phase 1 完成,内部测试
2. **Beta**: Phase 2 完成,与选定用户进行用户测试
3. **RC**: Phase 3 完成,最终完善
4. **Release**: 所有阶段完成并测试通过
